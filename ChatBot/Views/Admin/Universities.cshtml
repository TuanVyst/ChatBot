@model IEnumerable<BusinessObject.Entities.University>

@{
    ViewData["Title"] = "Manage Universities";
}

<h1>Manage Universities</h1>

<p>
    <a asp-action="CreateUniversity" class="btn btn-primary">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Code</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Code</td>
                <td>
                    <a asp-action="EditUniversity" asp-route-id="@item.Id">Edit</a> |
                    <form asp-action="DeleteUniversity" asp-route-id="@item.Id" method="post" style="display:inline;">
                        <button type="submit" class="btn btn-link" onclick="return confirm('Are you sure you want to delete this university?');">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
